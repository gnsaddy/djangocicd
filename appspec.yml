version: 0.0
os: linux
files:
   - source: .
     destination: /home/ubuntu/djangocicd/
hooks:
   AfterInstall:
      - location: ./install.sh
        timeout: 300
        runas: root
   # ApplicationStart:
   #  - location: scripts/start_server
   #    timeout: 300
   #    runas: root
